<h1>Creatures</h1>

<% @creatures.each do |f| %>
  <p>
    <strong>Name:</strong> <%= f.name %>
    <strong>Description:</strong> <%= f.description %>
    <%= link_to "Show", creature_path(f.id)%>
  </p>

  <% end %>
